@model Ledger.Models.Entities.LedgerEntity

@{
    Layout = null;
}

<tr>
    <td>
        @Html.DisplayFor(modelItem => Model.Ledger)
    </td>
    <td>
        @Html.TextBoxFor(modelItem => Model.LedgerDesc, new { data_id = Model.Ledger, size = 30, id =""})
    </td>
    <td>
        <input type="button" class="saveEditedLedger" data-id="@Model.Ledger" value="Save" />
        <input type="button" class="cancelEditingLedger" data-id="@Model.Ledger" value="Cancel" />
    </td>
</tr>
