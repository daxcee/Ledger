@model Ledger.Models.Entities.Account

@{
    Layout = null;
}

<tr>
    <td>
        @Html.DisplayFor(modelItem => Model.Id)
    </td>
    <td>
        @Html.DisplayFor(modelItem => Model.Desc)
    </td>
    <td>
        @Html.DisplayFor(modelItem => Model.Category)
    </td>
    <td>
        @Html.DisplayFor(modelItem => Model.Comment)
    </td>
    <td>
        <button type="button" class="btn btn-default btn-sm editAccount" data-id="@Model.Id">
            <span class="glyphicon glyphicon-edit"></span> Edit
        </button>
    </td>
</tr>
